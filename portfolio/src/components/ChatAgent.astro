---
---

<div id="chat-widget-container" class="fixed bottom-6 right-6 z-50 font-sans antialiased text-sm">
  
  <!-- Chat Window (Hidden by default) -->
  <div id="chat-window" class="hidden flex-col bg-stone-50/95 dark:bg-stone-900/95 backdrop-blur-xl border border-stone-200/50 dark:border-stone-800/50 rounded-2xl shadow-2xl w-[350px] max-w-[calc(100vw-3rem)] h-[500px] max-h-[calc(100vh-6rem)] overflow-hidden transition-all duration-300 origin-bottom-right scale-95 opacity-0">
    
    <!-- Header -->
    <div class="flex items-center justify-between px-4 py-3 border-b border-stone-200/50 dark:border-stone-800/50 bg-white/50 dark:bg-black/50 backdrop-blur-md">
      <div class="flex items-center gap-3">
        <div class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></div>
        <div>
          <h3 class="text-[10px] font-bold uppercase tracking-[0.2em] text-stone-900 dark:text-white leading-none">About the Work</h3>
          <span class="text-xs text-stone-500 dark:text-stone-400">Agent</span>
        </div>
      </div>
      <button id="close-chat" class="p-1.5 text-stone-400 hover:text-stone-700 dark:hover:text-stone-200 transition-colors rounded-full hover:bg-stone-200/50 dark:hover:bg-stone-800/50" aria-label="Close Chat">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>
      </button>
    </div>

    <!-- Message Area -->
    <div id="chat-messages" class="flex-1 overflow-y-auto p-4 pt-6 gap-y-2 flex flex-col items-start w-full">
      <!-- Initial Greeting -->
      <div class="chat-message agent-message opacity-0 translate-y-2 transition-all duration-500 w-full flex justify-start" style="animation: fadeUp 0.5s ease forwards 0.2s">
        <div class="bg-stone-200/50 dark:bg-stone-800/50 text-stone-800 dark:text-stone-200 rounded-2xl p-3 max-w-[85%] rounded-tl-sm leading-relaxed whitespace-pre-wrap text-left">I’m an AI guide trained on Ben’s writing, notes, and artistic methodology.
I’m here to provide context about the photographs, the themes behind them, and the ideas that shape the work.</div>
      </div>
      
      <!-- Suggested Prompts -->
      <div id="suggested-prompts" class="flex flex-col gap-2 opacity-0 translate-y-2 transition-all duration-500 w-full mb-4 items-end mt-2" style="animation: fadeUp 0.5s ease forwards 0.4s">
        <button class="suggested-prompt text-xs text-stone-600 dark:text-stone-300 bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 hover:bg-stone-50 dark:hover:bg-stone-800 px-4 py-2 rounded-2xl rounded-tr-sm transition-colors w-fit text-right shadow-sm">Contact Ben Hickman</button>
        <button class="suggested-prompt text-xs text-stone-600 dark:text-stone-300 bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 hover:bg-stone-50 dark:hover:bg-stone-800 px-4 py-2 rounded-2xl rounded-tr-sm transition-colors w-fit text-right shadow-sm">Who are his influences?</button>
        <button class="suggested-prompt text-xs text-stone-600 dark:text-stone-300 bg-white dark:bg-stone-900 border border-stone-200 dark:border-stone-800 hover:bg-stone-50 dark:hover:bg-stone-800 px-4 py-2 rounded-2xl rounded-tr-sm transition-colors w-fit text-right shadow-sm">Why did he start writing poetry?</button>
      </div>
    </div>

    <!-- Input Area -->
    <div class="p-3 bg-white/50 dark:bg-black/50 backdrop-blur-md border-t border-stone-200/50 dark:border-stone-800/50">
      <form id="chat-form" class="relative flex items-center">
        <input 
          type="text" 
          id="chat-input" 
          class="w-full bg-stone-100 dark:bg-stone-900 border border-stone-200 dark:border-stone-800 rounded-full px-4 py-2.5 pr-12 text-stone-900 dark:text-white placeholder-stone-400 focus:outline-none focus:ring-1 focus:ring-stone-400 dark:focus:ring-stone-600 transition-all text-sm"
          placeholder="Ask about the work..."
          autocomplete="off"
        />
        <button 
          type="submit" 
          id="send-button"
          class="absolute right-1.5 p-1.5 bg-stone-900 dark:bg-white text-white dark:text-black rounded-full hover:scale-105 transition-transform disabled:opacity-50 disabled:hover:scale-100 flex items-center justify-center"
          aria-label="Send Message"
        >
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="-translate-x-[1px] translate-y-[1px]"><line x1="22" y1="2" x2="11" y2="13"></line><polygon points="22 2 15 22 11 13 2 9 22 2"></polygon></svg>
        </button>
      </form>
    </div>
  </div>

  <!-- Floating Toggle Button -->
  <button id="chat-toggle" class="absolute bottom-0 right-0 p-4 bg-stone-900 dark:bg-white text-white dark:text-black rounded-full shadow-lg hover:shadow-xl hover:scale-105 transition-all duration-300 z-10 flex items-center justify-center group" aria-label="Toggle Chat">
    <svg id="chat-icon-open" class="w-6 h-6 transition-transform group-hover:scale-110" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg>
  </button>

</div>

<style>
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .chat-message.user-message {
    align-self: flex-end;
  }
  
  .chat-message.agent-message {
    align-self: flex-start;
  }

  .typing-indicator {
    display: flex;
    gap: 4px;
    padding: 8px 12px;
    align-items: center;
  }

  .typing-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background-color: currentColor;
    opacity: 0.4;
    animation: typing 1.4s infinite ease-in-out both;
  }

  .typing-dot:nth-child(1) { animation-delay: -0.32s; }
  .typing-dot:nth-child(2) { animation-delay: -0.16s; }
  
  @keyframes typing {
    0%, 80%, 100% { transform: scale(0); opacity: 0.4; }
    40% { transform: scale(1); opacity: 1; }
  }
</style>

<script>
  let isOpen = false;
  let isTyping = false;
  let conversationHistory: {role: string, content: string}[] = [];

  const toggleBtn = document.getElementById('chat-toggle');
  const closeBtn = document.getElementById('close-chat');
  const chatWindow = document.getElementById('chat-window');
  const chatForm = document.getElementById('chat-form') as HTMLFormElement;
  const chatInput = document.getElementById('chat-input') as HTMLInputElement;
  const messagesContainer = document.getElementById('chat-messages');
  const sendButton = document.getElementById('send-button') as HTMLButtonElement;

  function toggleChat() {
    isOpen = !isOpen;
    if (isOpen) {
      chatWindow?.classList.remove('hidden');
      chatWindow?.classList.add('flex');
      // small delay to allow display block to apply before animating opacity/scale
      setTimeout(() => {
        chatWindow?.classList.remove('scale-95', 'opacity-0');
        chatWindow?.classList.add('scale-100', 'opacity-100');
        chatInput?.focus();
      }, 10);
      toggleBtn?.classList.add('scale-0', 'opacity-0');
    } else {
      chatWindow?.classList.remove('scale-100', 'opacity-100');
      chatWindow?.classList.add('scale-95', 'opacity-0');
      toggleBtn?.classList.remove('scale-0', 'opacity-0');
      
      setTimeout(() => {
        chatWindow?.classList.add('hidden');
        chatWindow?.classList.remove('flex');
      }, 300); // match duration-300
    }
  }

  toggleBtn?.addEventListener('click', toggleChat);
  closeBtn?.addEventListener('click', toggleChat);

  function scrollToBottom() {
    if (messagesContainer) {
      messagesContainer.scrollTo({
        top: messagesContainer.scrollHeight,
        behavior: 'smooth'
      });
    }
  }

  function addMessage(content: string, sender: 'user' | 'agent', isHtml = false) {
    if (!messagesContainer) return;

    const messageDiv = document.createElement('div');
    const isUser = sender === 'user';
    messageDiv.className = `chat-message ${sender}-message opacity-0 translate-y-2 transition-all duration-300 w-full mb-4 flex ${isUser ? 'justify-end' : 'justify-start'}`;
    
    let innerClasses = '';
    if (isUser) {
      innerClasses = 'bg-stone-900 dark:bg-stone-100 text-white dark:text-stone-900 rounded-2xl p-3 max-w-[85%] rounded-tr-sm leading-relaxed whitespace-pre-wrap text-left';
    } else {
      innerClasses = 'bg-stone-200/50 dark:bg-stone-800/50 text-stone-800 dark:text-stone-200 rounded-2xl p-3 max-w-[85%] rounded-tl-sm leading-relaxed whitespace-pre-wrap text-left';
    }

    const displayContent = isHtml ? content : escapeHTML(content);
    messageDiv.innerHTML = `<div class="${innerClasses}">${displayContent}</div>`;
    messagesContainer.appendChild(messageDiv);
    
    // trigger animation
    setTimeout(() => {
      messageDiv.classList.remove('opacity-0', 'translate-y-2');
    }, 10);

    scrollToBottom();
  }

  function showTypingIndicator() {
    if (!messagesContainer) return null;
    
    const indicator = document.createElement('div');
    indicator.id = 'typing-indicator';
    indicator.className = 'chat-message agent-message w-full mb-4 flex justify-start';
    indicator.innerHTML = `
      <div class="bg-stone-200/50 dark:bg-stone-800/50 text-stone-800 dark:text-stone-200 rounded-2xl p-3 max-w-[85%] rounded-tl-sm text-left">
        <div class="typing-indicator">
          <div class="typing-dot"></div>
          <div class="typing-dot"></div>
          <div class="typing-dot"></div>
        </div>
      </div>
    `;
    messagesContainer.appendChild(indicator);
    scrollToBottom();
    return indicator;
  }

  function removeTypingIndicator(indicator: HTMLElement | null) {
    if (indicator && indicator.parentNode) {
      indicator.parentNode.removeChild(indicator);
    }
  }

  function escapeHTML(str: string) {
    return str.replace(/[&<>'"]/g, 
        tag => ({
            '&': '&amp;',
            '<': '&lt;',
            '>': '&gt;',
            "'": '&#39;',
            '"': '&quot;'
        }[tag] || tag)
    );
  }

  const suggestedPromptsContainer = document.getElementById('suggested-prompts');
  const suggestedPrompts = document.querySelectorAll('.suggested-prompt');

  suggestedPrompts.forEach(btn => {
    btn.addEventListener('click', () => {
      const message = btn.textContent;
      if (message && chatInput) {
        chatInput.value = message;
        // Creating and dispatching a submit event to trigger the main form handler
        const submitEvent = new Event('submit', { cancelable: true, bubbles: true });
        chatForm?.dispatchEvent(submitEvent);
      }
    });
  });

  chatForm?.addEventListener('submit', async (e) => {
    e.preventDefault();
    if (isTyping) return;

    const message = chatInput?.value.trim();
    if (!message) return;

    // Hide suggested prompts once the user starts chatting
    if (suggestedPromptsContainer && !suggestedPromptsContainer.classList.contains('hidden')) {
      suggestedPromptsContainer.classList.add('hidden');
    }

    // Add user message to UI
    addMessage(message, 'user');
    chatInput.value = '';
    chatInput.disabled = true;
    sendButton.disabled = true;
    isTyping = true;

    const indicator = showTypingIndicator();

    if (message.toLowerCase() === 'contact ben hickman') {
      setTimeout(() => {
        removeTypingIndicator(indicator);
        addMessage(`You can contact Ben using the <a href="/contact" class="underline hover:text-stone-500 transition-colors">contact section</a>.`, 'agent', true);
        chatInput.disabled = false;
        sendButton.disabled = false;
        chatInput.focus();
        isTyping = false;
      }, 600);
      return;
    }

    try {
      const response = await fetch('/api/chat', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ message, history: conversationHistory })
      });

      const data = await response.json();
      
      if (!response.ok) {
        throw new Error(data.details || data.error || 'API Error');
      }
      
      // Update history
      conversationHistory.push({ role: 'user', content: message });
      conversationHistory.push({ role: 'model', content: data.reply });
      
      // keep only last 10 messages for context so it doesn't get too bloated
      if (conversationHistory.length > 20) {
        conversationHistory = conversationHistory.slice(conversationHistory.length - 20);
      }

      removeTypingIndicator(indicator);
      addMessage(data.reply, 'agent');

    } catch (error: any) {
      console.error(error);
      removeTypingIndicator(indicator);
      addMessage(`Error: ${error.message || "I'm having trouble connecting to the network right now."}`, 'agent');
    } finally {
      chatInput.disabled = false;
      sendButton.disabled = false;
      chatInput.focus();
      isTyping = false;
    }
  });

  // Close when pressing escape
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && isOpen) {
      toggleChat();
    }
  });
</script>
